version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: <TASK_DEFINITION>
        LoadBalancerInfo:
          ContainerName: "backend"
          ContainerPort: 8080
# Hooks:
#   BeforeInstall:
#     - location: scripts/validate-dependencies.sh
#       timeout: 300
#   AfterAllowTestTraffic:
#     - location: scripts/health-check.sh
#       timeout: 180